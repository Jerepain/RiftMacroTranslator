@model IEnumerable<RiftMacroTranslator.Models.Translation1DB>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.en)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fr)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.de)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.en)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fr)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.de)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
